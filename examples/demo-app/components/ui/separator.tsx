import type { HTMLAttributes } from "react";

/** shadcn/ui Separator stub — UI library boundary */
export function Separator(props: HTMLAttributes<HTMLHRElement>) {
  return <hr data-shadcn="Separator" {...props} />;
}
