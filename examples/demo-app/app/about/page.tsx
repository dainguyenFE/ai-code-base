import { InfoCard } from "../../components/cards/InfoCard";

/** Level 2 route — static nested `/about` */
export default function AboutPage() {
  return (
    <main>
      <InfoCard title="About trace examples" badge="static" />
    </main>
  );
}
